node_modules/
package-lock.json

# Claude Code local session config (machine-specific; not shared).
.claude/

# Generated by a separate "TripAdvisor Plan B" build workflow. That agent now
# runs in its own git worktree (branch codex/plan-b, sibling folder
# ../pei-foodie-road-trip-planb) so it no longer shares this working tree; these
# dirs remain ignored in case a legacy run still writes here.
outputs/
scripts/
